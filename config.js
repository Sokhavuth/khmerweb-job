// config.js

export default async ()=>{
    let config = {
        siteTitle: 'ការងារ​យើង',
        pageTitle: '',
        route: '',
        message: '',
        maxPosts: 10,
    }

    return config
}